name: Self-hosted runner (scheduled)


on:
  repository_dispatch:
  schedule:
    - cron: "17 2 * * *"
  push:
    branches:
      - check_ds

jobs:
  deepspeed-ci:
    name: DeepSpeed CI
    uses: huggingface/infra-github-runners/.github/workflows/transformers-tests.yml@main
    with:
      job: run_all_tests_torch_cuda_extensions_gpu
      slack_report_channel: "#transformers-ci-daily-deepspeed"
    secrets: inherit
